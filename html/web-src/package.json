{
  "name": "warsmash-web-src",
  "version": "0.0.0",
  "private": true,
  "description": "Vite+Preact single-entry frontend for the Warsmash HTML5 port. Output goes to ./dist via npm run build; Gradle layers it over TeaVM engine artifacts in html/build/dist/webapp/.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --experimental-strip-types tests/launchConfig.test.mjs",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@poki/netlib": "0.0.18",
    "mdx-m3-viewer": "^5.12.0",
    "preact": "^10.24.3"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.0"
  },
  "type": "module"
}
